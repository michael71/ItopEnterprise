apply plugin: 'com.android.application'

android {
    compileSdkVersion 9
    buildToolsVersion "20.0.0"

    defaultConfig {
        applicationId "de.itomig.itopenterprise"
        minSdkVersion 9
        targetSdkVersion 21
    }

    buildTypes {
        release {

        }
    }
}

dependencies {
    compile 'com.android.support:support-v4:21.0.0'
    compile 'com.google.code.gson:gson:2.2.4'
}
